<x-app-layout><div class="space-y-5"><div><p class="text-sm text-black/45">Kasir</p><h2 class="mt-1 text-2xl font-semibold">Riwayat Shift</h2></div>@if(session('success'))<div class="rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-700">{{ session('success') }}</div>@endif<div class="overflow-hidden rounded-2xl border bg-white"><table class="w-full text-left text-sm"><thead class="border-b text-xs uppercase text-black/45"><tr><th class="px-5 py-4">Shift</th><th class="px-5 py-4">Buka</th><th class="px-5 py-4">Tutup</th><th class="px-5 py-4">Kas Seharusnya</th><th class="px-5 py-4">Kas Aktual</th></tr></thead><tbody class="divide-y">@forelse($shifts as $shift)<tr><td class="px-5 py-4 font-medium">{{ $shift->shift_number }}</td><td class="px-5 py-4">{{ $shift->opened_at->format('d/m/Y H:i') }}</td><td class="px-5 py-4">{{ $shift->closed_at?->format('d/m/Y H:i') ?? 'Aktif' }}</td><td class="px-5 py-4">Rp {{ $shift->expected_cash !== null ? number_format((float)$shift->expected_cash,0,',','.') : '-' }}</td><td class="px-5 py-4">Rp {{ $shift->closing_cash !== null ? number_format((float)$shift->closing_cash,0,',','.') : '-' }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-12 text-center text-black/40">Belum ada riwayat shift.</td></tr>@endforelse</tbody></table>@if($shifts->hasPages())<div class="border-t px-5 py-4">{{ $shifts->links() }}</div>@endif</div></div></x-app-layout>