<x-app-layout><div class="space-y-5"><div class="flex items-end justify-between gap-3"><div><p class="text-xs font-semibold uppercase tracking-wide text-black/40">Master Data</p><h1 class="mt-1 text-2xl font-bold">Satuan</h1><p class="mt-1 text-sm text-black/50">Kelola satuan produk yang digunakan dalam stok dan transaksi.</p></div><a href="{{ route('admin.units.create') }}" class="rounded-xl bg-[#171719] px-4 py-2.5 text-sm font-semibold text-white">Tambah Satuan</a></div>@if(session('success'))<div class="rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-700">{{ session('success') }}</div>@endif<div class="overflow-hidden rounded-2xl border border-black/5 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="border-b border-black/5 bg-black/[0.02]"><tr><th class="px-5 py-3">Kode</th><th class="px-5 py-3">Nama</th><th class="px-5 py-3">Deskripsi</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-black/5">@forelse($units as $unit)<tr><td class="px-5 py-4 font-medium">{{ $unit->code }}</td><td class="px-5 py-4">{{ $unit->name }}</td><td class="px-5 py-4 text-black/55">{{ $unit->description ?: '-' }}</td><td class="px-5 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $unit->is_active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-600' }}">{{ $unit->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td class="px-5 py-4 text-right"><div class="flex justify-end gap-2"><a href="{{ route('admin.units.edit',$unit) }}" class="rounded-lg border border-black/10 px-3 py-1.5 text-xs font-semibold">Edit</a><form method="POST" action="{{ route('admin.units.destroy',$unit) }}" onsubmit="return confirm('Hapus satuan ini?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-1.5 text-xs font-semibold text-red-600">Hapus</button></form></div></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-black/45">Belum ada satuan.</td></tr>@endforelse</tbody></table></div>@if($units->hasPages())<div class="border-t border-black/5 px-5 py-4">{{ $units->links() }}</div>@endif</div></div></x-app-layout>